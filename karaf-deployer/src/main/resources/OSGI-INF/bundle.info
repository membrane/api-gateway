h1. Synopsis

${project.name}

${project.description}

Maven URL:
[mvn:${project.groupId}/${project.artifactId}/${project.version}]

h1. Description

The membrane deployer allows you to drop Membrane file directly in the Karaf deploy directory.

Karaf supports Membrane configuration files.

h1. See also

Deployer - section of the Karaf User Guide.

Format of proxies.xml - see http://membrane-soa.org/esb-doc/ .