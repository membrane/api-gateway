openapi: '3.0.1'
info:
  title: API with API references
  version: '1.0.0'

paths:
  /foo:
    get:
      responses:
        '200':
          description: OK

  /ref-request:
    $ref: './references-request.yml#/paths/~1ref-request'