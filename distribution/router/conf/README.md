# Membrane Configuration

In this directory, you can find the Membrane configuration files. Usually you overwrite the 'apis.yaml' or 'proxies.xml' with your own configuration.

# Start
cd membrane-api-gateway-<VERSION>
./membrane.sh or ./membrane.cmd

