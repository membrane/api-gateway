global:
  - jsonProtection:
      maxTokens: 15
      maxSize: 110
      maxDepth: 3
      maxStringLength: 5
      maxKeyLength: 1
      maxObjectSize: 3
      maxArraySize: 3

---

api:
  port: 2000
  flow:
    - return:
        status: 200