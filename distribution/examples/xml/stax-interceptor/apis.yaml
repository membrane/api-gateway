# yaml-language-server: $schema=https://www.membrane-api.io/v7.4.0.json

components:
  staxInterceptor:
    bean:
      class: com.predic8.StaxConverterInterceptor

---

api:
  port: 2000
  flow:
    - request:
        - $ref: "#/components/staxInterceptor"
    - beautifier: {}
    - echo: {}

