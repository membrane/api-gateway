spec:
  port: 2000
  flow:
    - shadowing:
        targets:
          - target:
              host: localhost
              port: 3000
          - target:
              host: localhost
              port: 3001
          - target:
              host: localhost
              port: 3002
  target:
    host: api.predic8.de
    port: 443
    ssl: {}

---

spec:
  port: 3000
  flow:
    - log: {}
    - return:
        statusCode: 200

---

spec:
  port: 3001
  flow:
    - log: {}
    - return:
        statusCode: 201

---

spec:
  port: 3002
  flow:
    - log: {}
    - return:
        statusCode: 202