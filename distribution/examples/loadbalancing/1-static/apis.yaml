api:
  port: 8080
  flow:
    - balancer:
        clustersFromSpring:
          - clusters:
              - cluster:
                  name: Production
                  nodes: # Replace these with your backend nodes.
                    - node:
                        host: localhost
                        port: 4000
                    - node:
                        host: localhost
                        port: 4001
                    - node:
                        host: localhost
                        port: 4002

---
# Mock nodes for testing. Remove them in production.

api:
  port: 4000
  name: Node 1
  flow:
    - counter:
        name: Node 1

---

api:
  port: 4001
  name: Node 2
  flow:
    - counter:
        name: Node 2

---

api:
  port: 4002
  name: Node 3
  flow:
    - counter:
        name: Node 3