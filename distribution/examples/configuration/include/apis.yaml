# yaml-language-server: $schema=https://www.membrane-api.io/v7.1.2.json

include:
  - ./customers/customers.apis.yaml
  - ./orders/orders.apis.yaml

---
api:
  port: 2000
  flow:
    - template:
        contentType: application/json
        src: |
          {
            "message": "Not Found!"
          }
    - return:
        status: 404
