include:
  - nested/nested.apis.yaml

---
api:
  port: 2000
  path:
    uri: /from-file
  flow:
    - template:
        contentType: application/json
        src: |
          {
            "source": "from-file"
          }
    - return:
        status: 200
