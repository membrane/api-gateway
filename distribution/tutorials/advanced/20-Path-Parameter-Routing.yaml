# yaml-language-server: $schema=https://www.membrane-api.io/v6.3.11.json
#
# Membrane Tutorial: Path Parameter Routing
#
# Try:
#
#   curl http://localhost:2000/fruits/7

spec:
  port: 2000
  path:
    uri: /fruits/{id}
  target:
    url: https://api.predic8.de/shop/v2/products/${pathParam.id}
